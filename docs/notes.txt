Notes for next session:
  - Project pointer auto-advanced to Path 11 "Added Features" → M7 "User 
  settings infrastructure" (pending). Confirm that's the intended next
  focus when you resume (12 paths exist; the progression picked this
  post-Path-3).
  - Parked by your decision: host-backed defect-free-method gateway (notes
  129/131/143, host_state-backed) — its own future milestone, not started.
  - docs/notes.txt is now stale (it describes the pre-SQ5 plan).
  Authoritative record is the AIMFP DB (notes 138–146). Say the word next
  session and I'll refresh it.
  - Background Vite dev server still running on :5173 (task bf2fcgexe), per
   the usual leave-it-running convention. To stop it: kill that task or
  just close the terminal.