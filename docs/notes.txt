  flatpak uninstall org.chromium.Chromium
  flatpak uninstall --delete-data org.chromium.Chromium

  flatpak uninstall --unused   

  note #207

  Path: 4CHECK— Store Introspection
  Status: partial
  What's left: Redux ✅ (M11). Remaining: Zustand, Pinia, Jotai adapters —
    reuse the M11 ReduxStoreHandle-shaped plug-in contract (different
    detection + accessor, same MCP surface). Smallest,  highest-leverage — 
    the pattern already exists.
  DONE
  Deferred (documented, notes #199/#201/#203 + Jotai)
  ────────────────────────────────────────
  Path: 7 — Interaction Helpers & Filtering
  Status: partial
  What's left: Source-map ✅ (M13) + full filter pipeline ✅. Remaining:
    framework-aware action tools — react-synthetic-event click,
  find-and-act
     by component name/role (builds on the existing
    react_find_by_text/react_find_by_role), dispatch-form-submit.
  ────────────────────────────────────────
  Path: 5CHECK — Vue / Svelte / Solid Introspection
  Status: not built
  What's left: Vue 3 (vue.tree/vue.getState + Pinia), then Svelte/Solid
    where devtools hooks are stable. Largest — multiple  frameworks, 
    parallels the whole React module.
  ────────────────────────────────────────
  Path: 6 — Library Popup Capture
  Status: not built
  What's left: Auto-detect injected widgets
    (WalletConnect/RainbowKit/ConnectKit/Privy/SDK modals), capture
    shadow-root content + connect/auth failure events. Canonical test
  target
     is your crypto PWA.
  ────────────────────────────────────────
  Path: 10 — Firefox Port
  Status: deferred
  What's left: Manifest fork to Firefox MV3, chrome.*→browser.*. Kept on
  the
    roadmap so it isn't forgotten, but Firefox doesn't speak CDP, so
    chrome-devtools-mcp coexistence wouldn't apply — effectively out of
    scope for now.

Where things stand next: active path is now Path 9 (Public Release
  Orchestration), active milestone M45 (chrome-devtools-mcp
  coexistence polish) — but you've lined up Paths 6 & 7 + flatpak for
   next session.

PATH 9!
Confirmed live — but here's the important catch:
  chrome-devtools-mcp spawned its own browser (one page,
  about:blank). It is not attached to the Brave I launched
  on :9222 (which has the localhost:5173 React fixture). So
  right now the two MCPs are looking at different browsers:

  - pwa-debug → the Brave sandbox on :9222 (your real PWA /
  React fixture)
  - chrome-devtools → a separate isolated instance with a
  blank page

 What remains (not started — incremental)

  - M46 follow-on: audit the Zustand M36 connect-shim for the same
  impersonation risk (it still patches
  __REDUX_DEVTOOLS_EXTENSION__.connect; Zustand isn't context-based
  so fiber discovery can't replace it).
  - Task 97 (Jotai) is now unblocked → pending: its store discovery
  rides on the new collector; remaining is Jotai duck-typing +
  dev4_get_mounted_atoms enum, live-verified against your
  temp/react-jotai-main app.

  
  That's the project's whole coexistence model not being
  wired up. Per the design (and the next_steps both tools
  keep printing), chrome-devtools-mcp needs to be registered
   with --browserUrl pointing at our launched port so it
  attaches instead of launching its own:

  claude mcp add chrome-devtools --scope user -- npx -y
  chrome-devtools-mcp@latest --browserUrl
  http://127.0.0.1:9222

  Once it attaches to :9222, both MCPs operate on the same
  browser — pwa-debug for framework/store/page-world
  introspection, chrome-devtools for the CDP-parity surface
  — which is exactly the architecture locked for Path 9.

  Already complete: Capture (Path 2), Replay (Path 8, via M12), Browser
  launcher (Path 9, this session), the Added-Features path (Path 11:
  page-bridge/settings/disk-spill/test-isolation/readControls+filters/Redux
  /rrweb/sourcemap), plus the early foundation paths and React
  introspection.

  So, to reach 100%

  Four real work items remain — Path 4 remainder → Path 7 remainder → Path 
  5 → Path 6 — with Path 10 (Firefox) the one deferred/likely-skip. The
  natural next pickup is Path 4's other stores: it's the smallest and the
  plug-in pattern from M11 Redux is already in place.