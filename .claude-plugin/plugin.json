{
  "name": "pwa-debug",
  "version": "0.1.0",
  "description": "Browser-side debug layer that exposes live PWA state (DOM, framework state, stores, events, library popups) to AI agents via MCP — eliminating the human copy/paste loop when debugging web apps with AI.",
  "mcpServers": {
    "pwa-debug": {
      "command": "node",
      "args": [
        "${CLAUDE_PLUGIN_ROOT}/packages/host/dist/main.js"
      ]
    }
  }
}
