M4 progress (mid-flight): 5/10 items closed on task 4 (MCP↔NMH IPC bridge + first live tool).

Items closed:
  39 — Socket path resolution (socket_path.ts; POSIX XDG / Windows pipe)
  40 — IPC wire envelope + framing (envelope.ts; discriminated union, parser, frame reader wrapping native_messaging codec)
  41 — MCP-mode IPC server (ipc_server.ts; createIpcServer with connection map, pending requests, 5s default timeout, close + unlink)
  42 — NMH-mode IPC client (ipc_client.ts; createIpcClient with auto-register handshake)
  43 — NMH-mode bidirectional pump (nmh_mode.ts refactored; respondToMessage deleted, M3 hello dropped, transparent pump)

Module 4 mcp_ipc holds all 4 new IPC files. 144/144 tests passing.

Items remaining:
  44 — MCP-mode boot wiring + tool context (mkdir run/, start IPC server before SDK transport, thread ipcServer into ToolContext, close + unlink on shutdown)
  45 — session_ping live (resolves deferred note 12, file 49)
  46 — SW responder for session_ping (extension-side dispatch table)
  47 — host_status.activeConnections wiring (resolves deferred note 13, file 44; remove m3Note field)
  48 — IPC test coverage audit (most server/client/framing tests already exist; framing partial-frame, multi-frame, malformed already covered)
  49 — Brave acceptance retest (MANUAL; user rebuilds dist, restarts Claude Code so long-lived MCP-mode picks up new dist, calls session_ping + host_status)

Architecture in place:
  - MCP-mode = IPC server (long-lived, started by Claude Code MCP config)
  - NMH-mode = IPC client (transient, spawned by Chrome on connectNative)
  - Wire: 4-byte LE prefix + UTF-8 JSON body (reused from native_messaging)
  - Envelope: discriminated union { register | request | response | event }
  - register handshake captures extensionId at connection accept
  - request/response correlated by requestId, default 5s timeout
  - re-register from same extensionId replaces prior socket (Chrome respawn case)
  - On disconnect: rejects pending requests for that extensionId, fires onDisconnect

Items 44–48 are pure coding + unit-test work, no Claude restart needed.
Item 49 is the live-acceptance moment that requires a Claude restart.
