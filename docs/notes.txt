  flatpak uninstall org.chromium.Chromium
  flatpak uninstall --delete-data org.chromium.Chromium

  flatpak uninstall --unused   
===

 Milestone 48 (Path 9): pwa-debug Claude Code 
  plugin packaging — .claude-plugin/plugin.json
  (bundled mcpServers host entry), bundle the
  existing
  skills/chrome-devtools-coexistence/SKILL.md, decide
  whether to also declare chrome-devtools as a
  plugin mcpServer, + marketplace wiring. Exit:
  install as a plugin, /reload-plugins, MCP + skill
  live with no restart.

  Completing M48 finishes Path 9 (Public Release) —
  every other path-9 milestone is done.

  Beyond that, two paths are still marked pending but
  have no milestones decomposed yet (no committed
  work):
  - Path 7 — Interaction Helpers: framework-aware
  action tools (react synthetic-event click,
  find-and-act by component/role, form-submit). Core
  filtering + source-maps already shipped.